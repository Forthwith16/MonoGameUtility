namespace GameEngine.GUI.Components
{
	/// <summary>
	/// A wrapper placed around any GUI component.
	/// It allows for scrolling in the horizontal and/or vertical directions via a GUIRenderGroup.
	/// Note, however, that this parent class must be the GUI component added to the GUI hierarchy, not whatever GUI component it decorates.
	/// </summary>
	public class Scrollable// : GUIBase
	{

	}
}
